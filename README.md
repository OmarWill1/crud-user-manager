# CRUD User Manager

A simple full-stack user management system built with:

- **Backend:** FastAPI + SQLAlchemy + SQLite
- **Frontend:** HTML + JavaScript + CSS
- **Features:** Add, update, delete, and search users
